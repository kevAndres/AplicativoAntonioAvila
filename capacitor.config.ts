import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'idAntoAvila',
  appName: 'aplicacionIonic',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
